# 数字彩重构验收记录 · 2026-09-06

工程状态：**READY_FOR_JUDGMENT**。收益目标状态：**未证明可靠优于随机，也未证明净盈利**。用户尚未作最终 ACCEPTED 判定。

## 实际交付

新主应用 `index.html` 接入完整子集概率模型、七类候选、两彩预算和追加费用、实际期号、概率依据、逐期回测及前瞻台账。旧工具完整迁移至 `number-tools.html`，旧测试跟随旧入口继续运行。单文件构建覆盖全部四个页面。

双色球555期、大乐透553期真实历史；两彩最近各300期完整基础奖表。原始网络响应、来源时间、SHA-256、补源和更正保留在 `data/numbers`。福运状态按官方规则及连续奖池序列复核，555条中62开启、493关闭、未知0。规则推导不填造奖金。

发现并修复大乐透独立派奖行覆盖基础奖金额和注数的错误，依据同一原文更正35期后重新评估。26053期六等奖基本奖金15元，7.5元是独立派奖；官方2026活动要求单张单期至少18元，固定5注10元研究组合不应计入该派奖。详见 `data/numbers/discovery/dlt-promotion-parser-correction.md`。

## 当前120期回溯结果

每模型每彩种成本1200元，所有测试期票组可按公告基础奖表完整核算。以下为税前纸面回放，未模拟新增中奖者重新分配奖池：

| 模型 | 双色球净额 | 比随机差额 | 大乐透净额 | 比随机差额 |
|---|---:|---:|---:|---:|
| 随机基线 | -950 | 0 | -887 | 0 |
| 频率收缩 | -930 | +20 | -882 | +5 |
| 近期收缩 | -935 | +15 | -895 | -8 |
| 遗漏反转 | -965 | -15 | -914 | -27 |
| 转移收缩 | -955 | -5 | -899 | -12 |
| 混合模型 | -900 | +50 | -648 | +239 |
| 组合覆盖 | -785 | +165 | -939 | -52 |

所有候选均亏损。少亏不证明可靠优势；本窗口为历史探索，所有号码分布诊断也未达到预注册比较族阈值。完整逐期组合、训练截止、奖金与输入哈希见 `data/numbers/evaluation.json`，对应完整输入保存在 `evaluation-inputs/`。

## 真正开奖前登记

通过实际浏览器点击主界面登记，冻结协议 `numbers-prospective-v2`，协议哈希 `9cd93e1ab0d228e7a21ec6dd247488fd46ad7b3c28fddcb36c6fbfeabb7ad60d`：

| 彩种 / 期号 | 实际登记（北京时间） | 研究截止（北京时间） | 批次 |
|---|---|---|---|
| 双色球26103 | 2026-09-06 09:16:58.262 | 2026-09-06 18:15 | 7模型 × 5注 |
| 大乐透26102 | 2026-09-06 09:18:06.211 | 2026-09-07 18:25 | 7模型 × 5注 |

合计2个期次、14个模型组合、70注纸面票，真实结算为0，等待将来开奖。训练时间与实际生成/发布时刻分开，写盘跨截止不会回填。一次缓存重跑和一次实际网络日任务均新增0批次、修订0、待开奖2，保持原始票组。冷进程重复执行能够读取同一台账。

## 验证证据

- `npm run verify`：**389/389通过**，日志 `numbers-v2-verification.txt`。涵盖精确概率归一化与抽样、未来数据隔离、缺失奖金、追加预算、更正/撤回、截止时钟、真实并发原子登记、协议篡改、固定窗口和本机HTTP边界。
- 独立审查发现并验证修复：协议正文/时间元数据沿用旧哈希；开奖前采集的伪赛果；缺少到期开奖结果却触发分布结论；生成或落盘耗时导致登记回填。缺结果会暂停阈值判断，未开奖未来期不阻断。
- 真实网络 `node scripts/numbers-daily.js`：退出0、来源ok、原批次不重复；日志 `numbers-v2-live-daily.txt`。缓存幂等证据 `numbers-v2-repeat.txt`。
- 浏览器：双色球种子0生成合法五注与均匀概率；两彩实际登记；大乐透追加五注10元被拒且禁导出，改15元后混合模型生成成功；单文件包含实绩快照且禁写；浏览器错误/警告0。结构化记录 `numbers-v2-browser.json`。
- `npm run build` 完成、`git diff --check`通过；原足球6个冻结计算文件逐项哈希仍一致。
- 更新既有automation任务，保留每天北京时间10:30/18:30/23:30，依次执行足球与数字彩日任务并构建。更新状态ACTIVE已读回；尚未将未来调度触发或未发生的开奖结算报告为成功。

## 仍需新观察回答的内容

可靠收益优势尚未成立。高额浮动奖金的反事实分配、可适用活动资格和罕见大奖尾部不能由这120期简单自助抽样识别，自动盈利晋级保持关闭。下一期预告为有原文的次级公开来源，官方销售截止为空；研究采用更早的3小时锁定线。广东福运停止公告直连405，已区分官方索引阅读与HTTP200归档，其他官方规则与启动公告原文已归档。计算机与Codex需要可运行，错过的开奖前时间不补造。

## 交付自检

准确性4/5：核心数学、奖表修正与冷运行有证据，但实际反事实可兑奖收入未识别。完整性4/5：工程链路完整，真正未来结算与收益优势仍需新事件。清晰度4/5：主表同时显示绝对亏损和随机差额，复杂原文留在展开区。可操作性4/5：主应用、日任务、冻结台账均可用，调度依赖本机在线。简洁性4/5：界面集中到四步，旧工具另存。平均4.0；首要改进是积累真实前瞻结果、核验新规则和活动资格，而不是把既有负面证据包装成成功。
